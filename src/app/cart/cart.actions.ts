import { createAction } from '@ngrx/store';

export const add = createAction('[Cart Component] Add');
export const remove = createAction('[Cart Component] Remove');
export const reset = createAction('[Cart Component] Reset');